
public enum TriggerBehavior { HOLDWHILEINSIDE, TOGGLE }

// Ho fatto questo script perchè mi sono accorto in alcune classi che ereditano ITriggerable aveo bisogno di un enum che facesse la stessa cosa
// allora per non duplicarlo in entrambi gli script ho creato una classe statica dove ho tenuto appunto questo enun, in caso io abbia bisogno in futuro di un enum per
// altre cose triggerabili lo aggiungerei qui, cosi in caso io debba usarlo in più script possa non duplicarlo
